import { DateInWordsPipe } from './date-in-words.pipe';

describe('DateInWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new DateInWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
